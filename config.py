"""All env vars, constants, and the 100-prompt bank (populated in Phase 2)."""
# Imports planned:
# import os
# from dotenv import load_dotenv

# TODO: Load and expose all environment variables as typed constants
# TODO: Define ALLOWED_CHAT_IDS mapping (person name -> chat ID)
# TODO: Define 100-prompt bank (list of story prompt strings) in Phase 2
# TODO: Define schedule constants (cadence, times, timezones)

pass
